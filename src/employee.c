#include "../headers/employee.h"
#include <stdio.h>

void employeeMenu(char *username) {
  printf("Employee Menu Placeholder for %s\n", username);
}

void viewSalary(char *username) {
  printf("View Salary Placeholder for %s\n", username);
}

void updateSalary(char *username) {
  printf("Update Salary Placeholder for %s\n", username);
}

void deleteEmployee(char *username) {
  printf("Delete Employee Placeholder for %s\n", username);
}

void generatePayroll(char *username) {
  printf("Generate Payroll Placeholder for %s\n", username);
}

void viewPayroll(char *username) {
  printf("View Payroll Placeholder for %s\n", username);
}
